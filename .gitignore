# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
.venv*/
venv/
env/
.env
.env.local
.env.*.local

# Test caches & tooling
.mypy_cache/
.pytest_cache/
tests/e2e/e2e_screenshots/
.ruff_cache/
.coverage
htmlcov/
bandit_output.json
report.json

# Streamlit
.streamlit/secrets.toml
.streamlit/secrets.*.toml

# ───────────────────────────────────────────────────────────
# SECURITY: secrets & credentials (NEVER commit)
# ───────────────────────────────────────────────────────────
# API provider / editor configs that may contain real keys
api_configuration.json
api_providers.json
cline_config.json
auth.json
cline.log
*.api-key
*.api_key

# Generic patterns for accidental key dumps
**/secrets.json
**/secrets.yaml
**/secrets.yml
**/secrets.toml
**/*credentials*.json
**/*credentials*.yaml
**/*credentials*.yml
**/*.pem
**/*.key
**/service-account*.json
**/gcp-key*.json
**/aws-credentials*

# ───────────────────────────────────────────────────────────
# DATA: caches, models, dumps, logs (no regeneration value)
# ───────────────────────────────────────────────────────────
# PDF/scrape caches
data/cache/
data/rate_limiter.db
data/seed_validation.json
data/regression_*.html
data/regression_*.json
data/dispatch.json
data/prices_latest.json
data/cleanup_track.json
data/llm_cache.db
data/test_runs/

# ML/model artifacts (rebuilt by `scripts/export_onnx.py`)
data/embedding_cache/
data/onnx_models/
*.onnx
*.npy
*.npz
*.bin
*.safetensors
*.pt
*.pth
*.pkl
*.h5
*.safetensors
tokenizer.json
special_tokens_map.json
tokenizer_config.json

# Store backups (local snapshots, not for version control)
data/store_backups/

# Backups / dumps / SQL loose files
*.sql
!supabase/*.sql
backup_*.sql
*_prod_*.sql
*_staging_*.sql
dump.sql
pg_dump*.sql
trigger_fix.sql

# Editor / scratch files
git_msg.txt
*.patch
*.orig
*.swp
.DS_Store
Thumbs.db
.vscode/
.cursor/
.idea/
.cline/

# Tooling scratch
scripts/diagnose.py
cline.log
_tmp/
tmp/
skills-lock.json

# OpenCode skills — cache ignorado, skills versionadas por override
.opencode/skills/
!.opencode/skills/**
.github/workflows/skills-maintenance.yml

# Audit cache (regenerated locally — não versionar)
.agent/
data/audit/
data/skills_backup/

# Auto-generated skills doc — versionada (regenerada por sync_docs --sync)
# Não ignorar aqui; é truth source tracked como referencia coletiva dos skills.
# docs/skills.md

# Local backups / installer residue (não versionar — excedem GitHub 100MB limit)
CustoDoce.7z
C?Usersericsf/

# Setup files / downloaded artifacts
*.whl
miniconda.sh
pyenv_bashrc.txt

# ───────────────────────────────────────────────────────────
# SANEAMENTO: resíduos do working tree (scripts/sanitize.py)
# ───────────────────────────────────────────────────────────
C?Usersericsf/
CustoDoce.7z
skills-lock.json
data/audit/
data/skills_backup/

# Override: skills são versionadas (commit forçado)
!.opencode/skills/**

# Sanitize snapshots (rotacionados pelo script)
.archive/

# Generated manifests (committed - SSOT for mock validation in CI)

# E2E regression screenshots (test artifacts, not source)
data/regression_screenshots/
scripts/archive/

# Temporary dev helper scripts (underscore-prefixed, not production code)
scripts/_*.py
data/_*.jsonl
data/_coverage_result.jsonl
scripts/_local_test_barradoce.py
