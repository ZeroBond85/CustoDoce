# `dns_resolver` — API

> Última atualização: 2026-08-09 19:30 UTC
> Gerado por AST parsing dos serviços em `services/dns_resolver.py`.

## Funções Públicas (2)

### check_url_reachable(url: str, max_retries: int)

Check if a URL's hostname is resolvable via DNS (with retry).

### resolve_hostname(hostname: str, max_retries: int, delay: float)

Resolve a hostname to IP addresses with retry.

