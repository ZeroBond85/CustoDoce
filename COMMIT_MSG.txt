fix(scrapers): Carrefour crash + Chefon 429 session reuse + LLM auth

- CarrefourHybridScraper: implement abstract parse_products() (was crash on instantiation)
- Chefon/Shopify curl_cffi: persistent Session with cookie/TLS reuse across pages
  (was fresh connection per page = guaranteed Cloudflare rate-limit escalation)
- Chefon: jitter on per-page delays (+/-20%) and between-page sleeps (1-3s random)
- Chefon: reduce max_pages 20->12, base_delay 60->120 (fewer 429s, faster overall)
- flyer_hybrid/vision_strategies: only check GH_MODELS_TOKEN (not GITHUB_TOKEN,
  which lacks models/copilot scope = guaranteed 401 in CI)
- auto_promote_discovered_stores: replace rpc('exec_sql_query') with direct
  supabase-py table queries (RPC doesn't exist in Supabase)
