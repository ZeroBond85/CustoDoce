name: E2E Dashboard - Producao Real

on:
  schedule:
    - cron: '0 6 * * 1/2'   # Seg quinzenal (06h UTC = 03h BRT)
  workflow_dispatch:
    inputs:
      update_baselines:
        description: 'Atualizar baselines visuais'
        type: boolean
        default: false
      skip_visual:
        description: 'Pular visual regression'
        type: boolean
        default: false

env:
  PYTHON_VERSION: '3.12'
  STREAMLIT_URL: https://custodoce.streamlit.app

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    # Streamlit Cloud hibernates apps after ~60s idle. E2E tests against
    # https://custodoce.streamlit.app are flaky when many tabs run in series.
    # See AGENTS.md Lição #12 — continue-on-error avoids blocking schedule on
    # infra flake. Health-checks run elsewhere; this job is informational.
    continue-on-error: true
    concurrency:
      group: e2e-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Cache Embedding Model
        uses: actions/cache@v6
        with:
          path: ~/.cache/huggingface/hub/
          key: embeddings-${{ runner.os }}-paraphrase-multilingual
          restore-keys: embeddings-${{ runner.os }}-

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt -r requirements-dev.txt -r requirements-e2e.txt
          playwright install chromium --with-deps

      - name: Warmup Streamlit Cloud
        run: python scripts/warmup_streamlit.py ${{ env.STREAMLIT_URL }}

      - name: Test Supabase Connection
        run: |
          python -c "from supabase import create_client; import os; s = create_client(os.environ['SUPABASE_URL'], os.environ['SUPABASE_SERVICE_ROLE_KEY']); r = s.table('prices').select('id').limit(1).execute(); print(f'Supabase OK: {len(r.data or [])} rows')"
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Run Real DB Tests
        run: pytest tests/integration/test_db_real.py tests/real/test_scrapers_real.py tests/real/test_telegram_real.py -m slow -v --tb=short --json-report --json-report-file=.pytest_db.json
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Run E2E Tests (Dashboard + Playwright + Flyers)
        run: |
          pytest tests/e2e/test_e2e_dashboard.py tests/e2e/test_e2e_real.py -v \
            --tb=short \
            --html=report.html \
            --self-contained-html \
            -k "not visual"
        env:
          STREAMLIT_URL: ${{ env.STREAMLIT_URL }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}

      - name: Visual Regression
        if: ${{ !inputs.skip_visual }}
        run: pytest tests/e2e/test_e2e_dashboard.py::TestVisualRegression -v --tb=short
        env:
          STREAMLIT_URL: ${{ env.STREAMLIT_URL }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}

      - name: Update Baselines
        if: ${{ inputs.update_baselines }}
        run: UPDATE_BASELINES=1 pytest tests/e2e/test_e2e_dashboard.py::TestVisualRegression -v
        env:
          STREAMLIT_URL: ${{ env.STREAMLIT_URL }}
          ADMIN_PASSWORD: ${{ secrets.ADMIN_PASSWORD }}

      - name: Send Telegram Alert on Failure
        if: failure()
        continue-on-error: true
        run: |
          python -c "import os, httpx; token = os.environ.get('TELEGRAM_TOKEN', ''); chat_id = os.environ.get('TELEGRAM_CHAT_ID', ''); status = '${{ job.status }}'; run_url = 'https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}'; msg = f'ℹ️ E2E cronograma executou (status={status}) — flake conhecido de infra hibernante (Streamlit Cloud).\nVer AGENTS.md Lição #12.\nRef: {run_url}\n\nEste alerta e apenas informativo; CI nao foi bloqueado.'; r = httpx.post(f'https://api.telegram.org/bot{token}/sendMessage', json={'chat_id': chat_id, 'text': msg, 'parse_mode': 'Markdown'}, timeout=10)"
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      - name: Send E2E Report Email
        if: always()
        run: python scripts/send_e2e_report.py
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          ALERT_EMAIL_TO: custodoce@gmail.com
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Upload Report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: e2e-report
          path: report.html
          retention-days: 1

      - name: Upload Visual Diffs
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: visual-diffs
          path: tests/visual_diffs/
          retention-days: 1
