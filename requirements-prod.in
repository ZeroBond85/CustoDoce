pdfplumber>=0.11,<1.1
rapidfuzz
python-telegram-bot>=20.0,<23.0
pyyaml>=6.0
jsonschema
openpyxl
pandas>=1.0,<4.0
python-dotenv
supabase>=2.0,<5.0
streamlit==1.59.0
plotly>=6.0,<7.0
httpx>=0.28,<1.0
selectolax
pytesseract
pdf2image
PyJWT
sentence-transformers>=3.0.1
scikit-learn>=1.5,<2.0
groq>=0.10
optimum
requests>=2.34
structlog
opentelemetry-api
opentelemetry-sdk

# RapidOCR: OCR denso para encartes graficos (parsers/flyer_hybrid.py). Emite
# regioes com bounding-box que a reconstrucao geometrica de preco consome. Os
# modelos PP-OCRv6 vem no wheel (sem download em runtime), mas o engine de
# inferencia (onnxruntime) NAO e dependencia declarada do rapidocr — por isso e
# fixado explicitamente abaixo (sem ele o RapidOCR nao roda). Gated por
# features.ai.flyer_hybrid; degrada p/ vision se o import falhar. Nativo no 3.14.
rapidocr>=3.9,<4.0
onnxruntime>=1.22,<2.0

# Transitive deps explicitly declared (used directly in code)
# See: parsers/semantic_matcher.py, services/price_intelligence.py, scrapers/ocr.py
numpy>=2.4
transformers>=5.0
pillow>=12.0
joblib>=1.5

# cryptography, msgpack: removidos de direct deps — são apenas transitivos
# onnxruntime: RE-ADICIONADO como direct dep acima (engine do rapidocr, nao transitivo)
# torch: removido de direct dep — nunca importado diretamente, fica como transitivo de sentence-transformers

# psycopg2-binary NÃO deve estar aqui — AGENTS.md regra #4: NUNCA psycopg2 (usar RPC porta 443)
# Scripts locais legacy: pip install psycopg2-binary manualmente se necessário

# pyarrow: dependência transitiva de pandas/streamlit, mas PINADA explicitamente
# para garantir paridade total de ambiente (AGENTS.md regra #10). Sem o pin, o
# Streamlit Cloud instala versão flutuante diferente do lock local/CI e o
# st.dataframe falha com ArrowInvalid em colunas JSONB (Licao #51).
pyarrow==24.0.0

# charset-normalizer: PINADO porque 3.4.8 está YANKED do PyPI (yanked warning em CI).
# Usar 3.4.7 (última versão válida) até que uma versão >=3.4.9 seja publicada.
charset-normalizer==3.4.7
